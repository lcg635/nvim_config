return {
  enable_diagnostics = true,
  filesystem = {
    filtered_items = {
      hide_gitignored = false,
    },
    window = { mappings = { h = "close_node" } },
  },
  window = {
    mappings = {
        ["l"] = "toggle_node",
    }
  }
}
